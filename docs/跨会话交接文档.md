# 跨会话交接文档

> **项目**：AI 助理系统（PRJ-001 三角色升级）
> **工作区**：测试环境（`.env_type = test`，回调端口 5101）
> **基线版本**：台账 v0.1（2026-08-15 重建）
> **更新规则**：每完成原子任务 / 切换模型前刷新「工作断点」区。

---

## 1. 工作断点（新会话首先读取）

### 已完成
- ✅ 台账基线重建（`台账/` 26 个 CSV，UTF-8 with BOM，格式校验通过）
- ✅ 环境核验合规（`.env_type = test`，仅测试端口 5101/5102/5103）
- ✅ 三角色基线（chat/office/life）+ 105 项回归测试全绿（既有成果）
- ✅ **启动阶段产物补齐（2026-08-15）**：项目章程.md、01_干系人登记册（权力-利益四象限）、12_可行性评估（五维全 Go）、14_启动就绪检查单（Go 判定）
- ✅ **需求验收评审（2026-08-16）**：需求收集清单 27 项重建、追溯矩阵 27 项（断链率 11.1%）、五维评审报告 6 份、门禁判定 **Go**（存量验收通过，REQ-034~037 增量列缺陷）
- ✅ **REQ-034~037 增量开发（2026-08-16）**：跨会话记忆（`_memory_*` 事实提取/持久化/上下文注入）、任务委派（`#委派 办公|日程|闲聊`）、文档起草（`起草 <主题>` → `data/drafts/`）、主动提醒（callback_server 守护线程每 60s 轮询 `check_reminders`）；新增 14 项回归用例，**96/96 全绿**（REQ-DEF-001~004 关闭）
- ✅ **文档处理能力弱化改造（2026-08-16）**：新增 `external_doc_tools.py` 适配层，PPT 生成优先调用外部 **PPTAgent**（Docker+智谱，`pptagent generate`）→ **dashi-ppt**（Node HTML→PPTX 导出），摘要优先委托外部 LLM；`document_handler.py` 的 PPT/Word/Excel 分支改为外部优先、本地 core 兜底；新增 6 项回归用例，**102/102 全绿**

### 待办（当前会话）
- [x] **飞书实测四项能力 + 文档处理外部化（2026-08-17 E2E 实测 8/8 通过）**：记忆(规则提取+持久化+上下文注入)、委派(办公/日程/闲聊/未知→help 路由)、起草(命令→_draft→data/drafts 落盘)、到期提醒(check_reminders+REMINDER_TARGET_ID 投递接线)、`#办公 ppt`(PPTAgent 优先调用接线，外部化生效)；真实 webhook POST→记忆持久化验证通过。**前置修复：运行中的回调进程为 08-13 旧代码（缺 08-16 记忆/委派/起草能力），已 `restart_callback.sh` 重启(新 PID 78693, 21:33)生效**。注：本次为本地 E2E 捕获实测（webhook 同款函数 + 补丁 send_message 捕获回复），非真人可见飞书投递（5101 无对外隧道、无配置 open_id）。
- [x] 台账 11_质量缺陷 更新 REQ-DEF-001~004 → 已修复（2026-08-17 补归档列"E2E 实测通过关闭"）
- [x] SRS 07 质量属性等章节明细补齐、需求冲突消解记录、威胁建模文档（REQ-DEF-005~007）：07_质量属性.csv 补 17 项明细；新建 冲突消解记录.csv（CONF-001~005）；新建 威胁建模.md（STRIDE 九项闭环）；缺陷台账 REQ-DEF-005~007 置已修复并归档
- [x] 更新 `AGENTS.md` 权威参考失效路径（架构资产真实状态 01/02 已建·03 缺失·04~06 空目录；SRS 07+冲突消解已补），同步交接文档风险卡点与权威参考表（2026-08-17）
- [x] **SRS 需求明细重建 + 追溯断链清零（2026-08-18）**：SRS 04_功能需求由 10 项合并 REQ 重建为 **27 项模块粒度全覆盖**；SRS 10_追溯矩阵重建 27 行与台账 08 对齐；需求收集清单 CHAT-09/10/11、LIFE-07 状态 待确认→已实现（E2E 8/8 依据）；09_附录 版本日志补 v1.2；四表一致校验断言 **断链率 0.0%**（备份于 `env_backups/SRS重建_2026-08-18/`）
- [x] **需求 v1.2 评审 Go + 基线固化（2026-08-18）**：六维评审 6 份（功能/缺陷/范围/范围跟踪/规范/风险安全）；门禁全通过（四表一致、优先级/MoSCoW 0 项不匹配、验收量化 27/27、CONF 全消解）；REQ-DEF-017 整改闭环（CHAT-04/05/06/07/SHARED-04 升"高"、CHAT-09 收集清单 Must）；门禁验收记录追加"需求(v1.2) Go"，**需求基线固化 v1.2**
- [x] **架构增量核对 + 基线刷新（2026-08-18）**：架构设计对需求 v1.2 全 27 项覆盖无断链；修正 06_架构基线（§2 纳入需求 v1.2、§3 删除过时"三可切换"对齐 ADR-007 单一 ollama）；05_ATAM 报告表述一致化 + 补增量核对记录；**ARCH-BASELINE v1.0→v1.1 固化**（备份 `env_backups/架构基线刷新_2026-08-18/`）
- [x] **架构资产对齐需求 v1.2 全量清理（2026-08-18~19）**：系统上下文图/接口设计文档/容器视图/数据架构设计/安全架构设计/架构设计文档/组件设计文档 7 份全部裁剪为三角色 + 单一 ollama（ADR-007），清 file/sys 残留与三后端表述、孤儿代码块、场景重编号（6.4 删除文件管理 → 6.4/6.5 降级与守护）；03_决策 ADR 历史留痕保留；终扫零残留
- [x] **代码层 file 助手清理（2026-08-19，方案1 已批准）**：删除 `assistants/file-assistant/`（src + venv）与 `assistants/dev-assistant/`（venv 空壳）；`callback_server.py` 移除 BACKENDS/file/sys 代理路由（`/webhook_file`/`/health_sys` 等全部 404）并升 v1.2；`regression_test.py` 移除 file/sys 测试段 + 回调编号 7→5 → **88/88 全绿**；`feishu_e2e_test.py` 移除 4号/5号 服务段 v2.0；删遗留脚本 start_file_bot/start_file_sys_bots/stop_file_sys_bots/ngrok_file.yml；`whitelist.yaml` 删 file/dev 段；`diagnose.py/.sh` 移除 dev 检查；`setup_test_env.sh` 删 venv-file 段；`update_docs.sh` dev 引用修正。**备份 `backups/20260818_file_cleanup/`，git 可回滚**。回调已重启，`/health` 正常
- [x] **update_docs.sh 僵尸脚本删除（2026-08-19，方案A 已批准）**：脚本输出目标 4 个文档路径已废弃、依赖脚本已删除、内容为 llama.cpp/8080 时代且尾部 echo 碎裂，仅被 diagnose.py 存在性校验。已删除 `scripts/update_docs.sh` 并从 `diagnose.py` critical_scripts 移除；diagnose 运行正常（仅既有 ngrok 未运行项）

### 待办（当前会话）

### 风险卡点
- ✅ SRS 需求明细已重建（2026-08-18，27 项全覆盖+追溯清零；原 29 项中五角色期作废/合并项按 PRJ-001 收敛归入 09_附录 历史遗留）
- 架构资产 01~06 已齐备（03_决策 ADR-001~006 / 04_原型验证 PV-01~08 / 05_架构评审 ATAM-Go / 06_架构基线 v1.0 均于 2026-08-17 补齐）
- 文档处理外部化依赖外部工具就绪：PPTAgent 需 Docker 存活 + 智谱 key（config.yaml 在外部项目，本项目不读不存）；dashi-ppt 首次渲染需装依赖；单次生成 5-10 分钟，飞书侧已用后台线程处理

---

## 2. 当前架构快照

| 角色 | 目录 | 入口 |
|------|------|------|
| 1号AI | `assistants/chat-assistant/` | `message_handler.process_message()` |
| 2号AI | `assistants/office-assistant/` | `document_handler` + `external_doc_tools`（外部化） |
| 3号AI | `assistants/life-assistant/` | `process()` |
| 共享 | `shared/` | `callback_server.py`（:5101） |

## 3. 权威参考

| 内容 | 位置 | 状态 |
|------|------|------|
| 需求/SRS | `requirements/SRS_v1.0/`（v1.2，27 项功能明细+追溯清零） | 已齐备（2026-08-18） |
| 需求追溯矩阵 | `台账/08_需求追溯矩阵.csv` | 已建（明细待补） |
| 台账 | `台账/`（00~25） | 已建 |
| 架构资产 | `01_架构策略` `02_架构设计` `03_决策` `04_原型验证` `05_架构评审` `06_架构基线` | 已齐备（03~06 补齐，2026-08-17；基线 v1.1 刷新 2026-08-18） |
| 设计汇总 | `docs/design_summary.md` | 存在 |
| 环境核验 | `python3 scripts/diagnose.py` | 可用 |

---

**版本**：v0.2　**最后更新**：2026-08-19　**维护人**：role-governance
